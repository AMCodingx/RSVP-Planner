[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."keep-alive"]
  schedule = "0 0 * * 0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
